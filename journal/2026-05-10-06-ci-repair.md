---
date: 2026-05-10
session: 06
slug: ci-repair
sprint: 5
duration_minutes: 20
files_touched:
  - journal/2026-05-10-06-ci-repair.md
  - docker-compose.yml
  - .github/workflows/ci.yml
acceptance_rows_closed: []
acceptance_rows_in_progress: [C03]
---

## PLAN

Repair the two failing GitHub Actions jobs from run `25626479911`: `build-test` and `trivy`. The failure logs show `sandbox-api` becomes unhealthy when CI sets `API_TOKEN`, and the Trivy action pin `aquasecurity/trivy-action@0.24.0` no longer resolves.

- [ ] Make the API container healthcheck send `X-API-Token` when `API_TOKEN` is present.
- [ ] Update Trivy action pin to a resolvable release.
- [ ] Run local `make ship-check`.
- [ ] Commit, push, and verify the new CI run.

Targeted rows: C03, C04

## TEACH-BACKS

### TEACH-BACK: Keep auth on `/health`, fix the healthcheck caller

**Context.** CI sets `API_TOKEN=ci-test-token-do-not-reuse`, so the API middleware requires `X-API-Token` even for `/health`. Compose healthcheck did not send it, which made `sandbox-api` unhealthy and blocked Nginx startup.

**Alternatives considered**
1. **Exempt `/health` from API auth** — convenient for liveness probes, but it weakens the contract wording that token auth applies to every request when enabled.
2. **Send the token from the compose healthcheck** — keeps auth behavior strict and fixes the actual caller.

**Chosen** — **send the token from the compose healthcheck**, because it preserves the API contract while making CI and authenticated deployments boot cleanly.

**Failure modes**
- A malformed `.env` with duplicate `API_TOKEN` lines could still confuse Compose; CI currently appends after `.env.example`, and Docker Compose uses the final value in practice.
- Public `/health` returns 401 when auth is enabled, which is acceptable for an authenticated API health endpoint.

**Reversal cost.** Low.

**Citations.**
- GitHub job `75222464223`: `container sandbox-api is unhealthy`
- `governance/10_API_CONTRACT.md` Authentication

## NOTES

- `gh api repos/aquasecurity/trivy-action/releases/latest --jq .tag_name` returned `v0.36.0` on 2026-05-10, so the workflow now pins that resolvable release.

## OUTCOMES

```
$ make ship-check
PASS
- pre-commit run --all-files: passed
- bash ci/shellcheck.sh: passed
- make test-api: passed, 14 requests, 59 assertions, 0 failures
```

```
$ API_TOKEN=ci-test-token-do-not-reuse make up equivalent
PASS - sandbox-api healthcheck reached healthy when the compose healthcheck sent X-API-Token.
```

## LEARNINGS

- Compose healthchecks run inside the container and must authenticate exactly like any other API caller when `API_TOKEN` is enabled.
- `aquasecurity/trivy-action@0.24.0` is no longer resolvable; `v0.36.0` is the current release tag returned by GitHub's release API.

## BLOCKERS

none

## NEXT

Push the CI repair commit and confirm GitHub Actions build-test and trivy are green. If Trivy finds real HIGH/CRITICAL CVEs after the action resolves, fix the image base or dependency pins rather than weakening the scan.
